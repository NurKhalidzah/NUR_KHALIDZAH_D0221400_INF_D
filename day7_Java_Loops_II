int sum = a;
            for(int j=0;j<n;j++){
                //sum = (sum + (int) Math.pow(2, j) * b);
                sum += ((int) Math.pow(2, j) * b);
                System.out.print(sum+" ");
            }
